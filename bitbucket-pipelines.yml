pipelines:
  tags:
    v*:
    - step:
        name: faspay service deployment
        deployment: production
        caches:
          - node
          - docker
        script:
          - set +e
          - bash ./deploy/bitbucket-deploy.sh
        services:
          - docker
