pipelines:
  tags:
    v*:
    - step:
        name: faspay service deployment
        deployment: production
        caches:
          - node
          - docker
        script:
          - set +e
          - ./deploy/bitbucket-deploy.sh
        services:
          - docker
